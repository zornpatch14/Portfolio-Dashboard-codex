# Test Data

Place small representative TradeStation XLSX trade list and MTM files here for baseline/parity tests. Keep sizes modest so CI remains fast. Update `tests/baseline/selections.json` to reference filenames you add.
